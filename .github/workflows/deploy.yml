import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  // GitHub PagesのURL（/kanto-yard-map/）に合わせてベースパスを設定
  base: '/kanto-yard-map/',
})